<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.algonquinlive.cst335.finalgroupproject">

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
      <activity android:name=".StartActivity">
                  <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
      </activity>
        <activity android:name=".patient_intake.PatientIntakeStart" />
        <activity android:name=".patient_intake.PatientIntakeForm" />
        <activity android:name=".patient_intake.PatientIntakeFormDoctor" />
        <activity android:name=".patient_intake.PatientIntakeFormDentist" />
        <activity android:name=".patient_intake.PatientIntakeFormOptometrist" />
        <activity android:name=".patient_intake.PatientIntakeImport" />
        <activity android:name=".patient_intake.PatientIntakePatients"></activity>

        <!-- Multiple Choice Creator Activities - Prefix: Mcc -->
        <activity android:name=".MccMainActivity" />
        <!-- End Multiple Choice Creator Strings -->

        <activity android:name=".MccQuestionActivity"/>

        <activity
            android:name=".octranspo.AC_OCTranspActivity"
            android:theme="@style/Theme.AppCompat" />
    </application>

</manifest>